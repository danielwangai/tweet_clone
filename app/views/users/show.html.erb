<!-- <div class="span8">
	<% if @user.tweets.any? %>
	<h3>tweets (<%= @user.tweets.count %>)</h3>
	<ol class="tweets">
	</ol>
	<% end %>

	<% if signed_in? %>
		<section>
			<%= render 'shared/stats' %>
		</section>
		<%= render 'follow_form' %>
	<% end %>

	<%= render 'follow_form' %>
</div> -->
<% if signed_in? %>

	<div class="flood col-md-12">
		<div class="useravatar col-md-6">
	    	

	    </div>
	    
	</div>
	<div class="user_details col-md-4">
	    	<div>	    		
	    		<h2><%= @user.email %></h2>
	    	</div>
	    	<div>
	    		<%= render 'shared/stats' %>
	    		<%= render 'follow_form' %>
	    	</div>
	    	<div></div>

	    </div>
<% end %>

